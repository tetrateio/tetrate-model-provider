# Store the API key

Run **Tetrate Agent Router: Set Agent Router API Key** and paste the key.

The key is held in VS Code secret storage, scoped to the endpoint host. On macOS that is the system Keychain. It is never written to a settings file, a log, or the output channel, and it does not sync across machines.
